"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {AppShell} from "../../../components/app-shell";import {Badge,Card,PageHeader} from "../../../components/ui";
export default function NewProjectPage(){const [step,setStep]=useState(1);const router=useRouter();return <AppShell title="New Project"><PageHeader title="New production" subtitle="Create the job, configure automation, then start research"/><div className="grid two"><Card><div className="label">STEP {step} / 3</div>{step===1&&<><h3>Project</h3><label className="label">Project name</label><input className="input" defaultValue="$6 Handpowered Washer" style={{width:'100%',margin:'6px 0 14px'}}/><label className="label">Topic</label><textarea className="input" defaultValue="How to build a powerful handpowered washer for under six dollars." style={{width:'100%',minHeight:110,marginTop:6}}/><div className="actions" style={{marginTop:14}}><button className="btn primary" onClick={()=>setStep(2)}>Continue</button></div></>}{step===2&&<><h3>Production settings</h3><div className="grid three"><div><div className="label">Target duration</div><input className="input" defaultValue="04:00" style={{width:'100%'}}/></div><div><div className="label">Language</div><select className="input" style={{width:'100%'}}><option>English</option></select></div><div><div className="label">Automation</div><select className="input" style={{width:'100%'}}><option>ASSISTED</option><option>MANUAL</option><option>AUTOMATIC</option></select></div></div><div className="kv"><span>Voice</span><b>Adam · Documentary</b></div><div className="notice">Approval gates: Visuals + Final video</div><div className="actions" style={{marginTop:14}}><button className="btn" onClick={()=>setStep(1)}>Back</button><button className="btn primary" onClick={()=>setStep(3)}>Create project</button></div></>}{step===3&&<><h3>Project created</h3><Badge type="processing">RESEARCH QUEUED</Badge><p className="sub">The project is now waiting on backend job state. No artifact is shown as complete until the server reports it.</p><button className="btn primary" style={{marginTop:12}} onClick={()=>router.push('/projects/demo')}>Open project</button></>}</Card><Card><h3>Production policy</h3><div className="kv"><span>Versioning</span><b>Immutable</b></div><div className="kv"><span>Provenance</span><b>Scene → Attempt → Asset</b></div><div className="kv"><span>Credentials</span><b>Backend only</b></div><div className="kv"><span>Publication</span><b>Approval gated</b></div></Card></div></AppShell>}
