import {AppShell} from "../../../../components/app-shell";import {Badge,Card,PageHeader} from "../../../../components/ui";
export default function ThumbnailPage(){return <AppShell title="Thumbnail"><PageHeader title="Thumbnail Studio" subtitle="Versioned thumbnail candidates for the publication artifact" action={<button className="btn primary">Generate candidates</button>}/><div className="candidateGrid">{['$6 WASHER','BUILD FOR $6','NO ELECTRICITY'].map((x,i)=><Card key={x}><div className="candidatePreview" style={{fontSize:18,fontWeight:800}}>{x}</div><div className="actions" style={{marginTop:10}}><Badge type={i===1?'success':'pending'}>{i===1?'SELECTED':'CANDIDATE'}</Badge><span className="label">v{i+1}</span></div><button className="btn" style={{marginTop:10}}>Select</button></Card>)}</div></AppShell>}
