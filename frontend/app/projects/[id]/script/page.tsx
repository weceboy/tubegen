"use client";import {useState} from "react";import {AppShell} from "../../../../components/app-shell";import {Badge,Card,PageHeader,Progress} from "../../../../components/ui";
const versions=[['v3','Current','APPROVED','4m ago'],['v2','Previous','SUPERSEDED','18m ago'],['v1','Initial','SUPERSEDED','42m ago']];
export default function ScriptPage(){const [v,setV]=useState(0);const [body,setBody]=useState('Imagine doing laundry anywhere, anytime. Most portable washers are expensive and unreliable. Here is how I built a powerful one for under six dollars. You will need a bucket, PVC pipe, a few fittings and a hand crank. Let’s start by drilling holes into the bucket bottom.');return <AppShell title="Script"><PageHeader title="Script" subtitle="$6 Handpowered Washer · immutable version history" action={<div className="actions"><button className="btn">Generate</button><button className="btn">Regenerate</button><button className="btn primary">Approve</button></div>}/><div className="split"><Card><div className="label">VERSION HISTORY</div><div className="versionList" style={{marginTop:8}}>{versions.map((x,i)=><div className={`version ${v===i?'active':''}`} onClick={()=>setV(i)} key={x[0]}><div style={{display:'flex',justifyContent:'space-between'}}><b>{x[0]}</b><Badge type={i===0?'success':'pending'}>{x[2]}</Badge></div><div className="sub">{x[1]} · {x[3]}</div></div>)}</div><div className="notice" style={{marginTop:12}}>Older versions remain inspectable and are never silently overwritten.</div></Card><div className="grid"><Card><div className="label">SCRIPT EDITOR · {versions[v][0]}</div><textarea aria-label="Script editor" className="input" value={body} onChange={e=>setBody(e.target.value)} style={{width:'100%',minHeight:390,marginTop:10,lineHeight:1.7,resize:'vertical'}}/><div className="actions" style={{marginTop:10}}><button className="btn">Save</button><button className="btn">Create version</button></div></Card><div className="grid three"><Card><div className="label">WORD COUNT</div><div className="metric">{body.trim().split(/\s+/).length}</div></Card><Card><div className="label">EST. DURATION</div><div className="metric">03:42</div></Card><Card><div className="label">LANGUAGE</div><div className="metric">EN</div></Card></div><Card><Progress value={92} label="Script readiness"/><div className="kv"><span>Status</span><Badge type="success">APPROVED</Badge></div><div className="kv"><span>Target</span><b>4:00</b></div><div className="kv"><span>Version</span><b>{versions[v][0]}</b></div></Card></div></div></AppShell>}
