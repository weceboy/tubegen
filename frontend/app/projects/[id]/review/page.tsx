"use client";import {useState} from "react";import {AppShell} from "../../../../components/app-shell";import {Badge,Card,PageHeader,Table} from "../../../../components/ui";
export default function ReviewPage(){const [comment,setComment]=useState('');const [approved,setApproved]=useState(false);return <AppShell title="Review"><PageHeader title="Approval Center" subtitle="$6 Handpowered Washer · review gates are explicit" action={<Badge type={approved?'success':'warning'}>{approved?'APPROVED':'WAITING FOR REVIEW'}</Badge>}/><div className="grid two"><Card className="approval"><h3>Visual Approval</h3><div className="kv"><span>Reviewer</span><b>Alex</b></div><div className="kv"><span>Created</span><b>Today · 16:42</b></div><div className="kv"><span>Scope</span><b>16 scenes · 3 require review</b></div><div className="notice" style={{marginTop:12}}>Scene 03 and Scene 11 need visual approval before render can proceed.</div><div className="actions" style={{marginTop:14}}><button className="btn primary" onClick={()=>setApproved(true)}>Approve visuals</button><button className="btn">Reject</button></div></Card><Card><h3>Final Video Approval</h3><div className="kv"><span>Render</span><Badge type="pending">NOT RENDERED</Badge></div><div className="kv"><span>Visual gate</span><Badge type={approved?'success':'warning'}>{approved?'APPROVED':'PENDING'}</Badge></div><div className="kv"><span>Final decision</span><Badge type="pending">PENDING</Badge></div><button className="btn primary" disabled={!approved} style={{marginTop:14}}>Approve final video</button><div className="sub" style={{marginTop:8}}>Blocked until a successful render exists.</div></Card></div><Card style={{marginTop:12} as any}><h3>Decision comment</h3><textarea className="input" value={comment} onChange={e=>setComment(e.target.value)} placeholder="Required when rejecting" style={{width:'100%',minHeight:100,marginTop:8}}/><div style={{marginTop:12}}><button className="btn">Save comment</button></div></Card><div style={{marginTop:12}}><Card><h3>Approval history</h3><Table headers={['Stage','Status','Reviewer','Comment','Time']} rows={[["Visuals",<Badge type="warning">PENDING</Badge>,'Alex','Scene 03 / 11 need review','16:42'],["Final video",<Badge type="pending">BLOCKED</Badge>,'—','Waiting for render','—']]}/></Card></div></AppShell>}
